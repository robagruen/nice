const nice = (options) => {
    let carousel;
    let carouselOptions;
    function init(carouselOptions) {
        this.carouselOptions = carouselOptions;
    }
    init(options);
};
export default nice;
